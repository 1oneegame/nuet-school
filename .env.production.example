# Production Environment Variables for Vercel Deployment
# Copy this file and update the values for your production environment

# Next.js Configuration
NEXT_PUBLIC_URL=https://your-app.vercel.app

# MongoDB Atlas (Replace with your Atlas connection string)
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/nuet-app?retryWrites=true&w=majority

# NextAuth Configuration
NEXTAUTH_URL=https://your-app.vercel.app
# Generate a new secret for production using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NEXTAUTH_SECRET=your-production-nextauth-secret-here

# JWT Configuration
# Generate a new secret for production using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-production-jwt-secret-here

# Email Configuration (if using email features)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com

# Optional: Additional security configurations
# NODE_ENV=production (automatically set by Vercel)
# VERCEL=1 (automatically set by Vercel)

# Instructions:
# 1. Set up MongoDB Atlas and get your connection string
# 2. Generate secure secrets for NEXTAUTH_SECRET and JWT_SECRET
# 3. Update NEXT_PUBLIC_URL and NEXTAUTH_URL with your actual Vercel domain
# 4. Configure email settings if using email functionality
# 5. Add these variables to your Vercel project settings